# Core dependencies
vllm>=0.17.0
torch>=2.0.0
transformers>=4.35.0
datasets>=2.14.0,<4.0.0  # LiveCodeBench requires loading scripts, not supported in datasets 4.x

# Configuration and utilities
pyyaml>=6.0
tqdm>=4.65.0
numpy>=1.24.0

# synlogic verifier
markdown

# Data processing and analysis
pandas>=2.0.0

# Mathematical evaluation dependencies
sympy>=1.12  # Symbolic mathematics for answer equivalence checking
# latex2sympy2>=1.9.1  # LaTeX to SymPy conversion for math evaluation
# latex2sympy2_extended
# antlr4-python3-runtime==4.11.1  # Required for sympy LaTeX parsing (version sensitive)

# LIMO-compatible math grader dependencies
regex>=2023.0.0  # Enhanced regex for pattern matching (LIMO grader)
word2number>=1.1  # Convert word numbers to digits (optional, for LIMO grader)

# Logic task dependencies (KOR-Bench & SynLogic)
prettytable>=3.0.0  # KOR-Bench evaluation output
nltk>=3.8.0  # SynLogic text processing
math-verify>=0.5.0  # SynLogic math verification

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.14.0

ijson

accelerate
colorama>=0.4.6  
scipy>=1.10.0  

# Optional: for better logging and output
coloredlogs>=15.0
rich>=11.1.0  # Enhanced terminal output (optional but recommended)
anyio==4.8.0  # Async I/O support for vllm (optional)

ijson>=3.2.0  # Required for JSON parsing
# bigcodebench data convert
# appdirs>=1.4.4
# fire>=0.6.0
# multipledispatch>=0.6.0
# pqdm>=0.2.0
# tempdir>=0.7.1
# termcolor>=2.0.0
# tqdm>=4.56.0
# tree_sitter_languages>=1.10.2
# tree-sitter==0.21.3
# wget>=3.2

# datasets==2.18.0
latex2sympy2_extended==1.10.2
openai==1.99.9
# timeout for AMO symbolic solve now uses Python stdlib signal (no extra package)

# bigcodebench data convert
tree_sitter_python
pqdm